# audioVolume at 0.0 is silence, 1.0 is maximum volume
# this controls only the app volume, not computer audio volume
audioVolume: 0.5
videosExtension: "mp4"


#  don't change these
videosDirPath: "./videos" 
playScheduledVideos: false
nFramesToSkip: 3
camStreamWidth: 640
camStreamHeight: 360
tcpPort: 5011

# videos scheduling isn't fully implemented yet, don't change these
scheduledVideos:
  - 1.mp4
  - 2.mp4

